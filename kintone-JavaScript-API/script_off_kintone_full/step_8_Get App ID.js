(function(){
    'use strice'
    // ລະບົບຈະສະແດງ app id ອອກມາໃນ console 
    // system will display the app id in the console
    var appId = kintone.app.getId(); // It's getId, and not getID
    console.log(appId);

})();